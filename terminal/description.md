# terminal.cpp
### это моя первая задумка ОС.
### работает 75/25.
пробуйте!
__________________
# запуск.
### пропишите в терминале: `git clone https://github.com/DeltaDevSys/CPP`
### потом: `cd CPP/terminal`, `mkdir bin`, `g++ terminal.cpp -o bin/term`
### и последний шаг `bin/term`.
____________________
## при себе иметь:
* G++: `sudo apt install g++` или `sudo pacman -S g++`
* git: `sudo apt install git` или `sudo pacman -S git`
### после установки всего чуть выше перечиленного следуете пункту по запуску.
_____________________________________________________ 
